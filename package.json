{
  "name": "davidbramwell",
  "version": "1.0.0",
  "description": "My home page",
  "scripts": {
    "test": "ENVIRONMENT=test jasmine;",
    "postinstall": "for service in $(ls services/); do cd src/services/$service && npm install && cd -; done"
  },
  "author": ""
}
